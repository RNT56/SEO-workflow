# Remediation Rules

Classify every remediation as one of:

- `safe_auto_fix`
- `approval_required`
- `manual_strategy`
- `not_applicable`

Safe auto-fixes still require validation steps. Approval-required items must not be applied automatically.
