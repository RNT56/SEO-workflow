# @seo-polish/validation

Validates report files, evidence requirements, private URL leaks, safety flags and production
signal quality.

Strict validation checks the report contract, site intelligence artifacts, actionability on every
finding, browser-metric honesty, private URL handling and approval boundaries. Suppressions are
validated as non-destructive ledgers; findings remain visible in `findings.json`.
